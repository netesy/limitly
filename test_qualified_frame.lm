// Test: Try to use the qualified frame name directly
import test_module_frame as test;

print("=== Test Qualified Frame Name ===");
// Try calling with qualified name directly
var counter = test.Counter();
print("counter type set correctly");

// Access field directly
print("counter.count = {counter.count}");
