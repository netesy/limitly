// Test closure creation timing

fn make_simple_closure(x: int): function(): int {
    print("Inside make_simple_closure, x =", x);
    // This lambda should capture x
    return fn(): int { 
        print("Inside lambda, trying to access x");
        return x; 
    };
}

print("Testing closure creation timing:");
var closure = make_simple_closure(100);
print("Closure created successfully");