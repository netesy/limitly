// Test 3 outer, 2 inner
print("=== Testing 3 Outer, 2 Inner ===");

var outer_list = [1, 2, 3];
var inner_list = ["a", "b"];

print("Nested iter loops:");
iter (var x in outer_list) {
    print("Outer: {x}");
    iter (var y in inner_list) {
        print("  Inner: {y}");
    }
}

print("Done");