// Test named arguments in function calls

fn greet(name: str, age: int = 25, city: str = "Unknown") {
    print("Hello", name, "age", age, "from", city);
}

// Call with named arguments
greet("Alice", age: 30, city: "New York");

print("Named arguments test");