trait Number {
    fn get(): int;
}

frame BoxedInt {
    value: int;

    init(v: int) {
        this.value = v;
    }

    fn get(): int {
        return this.value;
    }
}

// Ensure the trait is implemented explicitly or implicitly. Wait, Limitly uses implicit/explicit traits? Let me check test files for traits.
