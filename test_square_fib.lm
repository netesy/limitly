fn square(x) {
    var result = x * x;
    return result;
}

fn fib(n) {
    if (n <= 1) {
        return n;
    }
    
    var a = 0;
    var b = 1;
    var i = 2
    
    while (i <= n) {
        var temp = a + b;
        a = b;
        b = temp;
        i = i + 1;
    }
    
    return b;
}

// Test calls
print("Square of 5:");
print(square(5));

print("Fibonacci of 10:");
print(fib(10));
