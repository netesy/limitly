// Test file for CST expression parsing with error recovery

// Valid expressions
var a = 1 + 2 * 3;
var b = -42;
var c = fn(arg1, arg2);
var d = obj.property;
var e = (a + b);

// Expressions with errors for recovery testing
var f = 1 + ;        // Missing right operand
var g = !;           // Missing unary operand  
var h = func(a, b;   // Missing closing parenthesis
var i = obj.;        // Missing property name
var j = (a + b;      // Missing closing parenthesis

print "Expression parsing test complete";