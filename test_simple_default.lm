// Simple test for default parameters
print("=== Simple Default Parameter Test ===");

// Function with default parameter
fn greet(name: str = "World"): str {
    return "Hello, " + name + "!";
}

print("Testing default parameter:");
print(greet());           // Should use default "World"
print(greet("Alice"));    // Should use provided "Alice"