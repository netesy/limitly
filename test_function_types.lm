// Test file for unified function type system

// Test 1: Basic function type annotation
var callback: fn(int, str): bool;

// Test 2: Named parameter function type
var processor: fn(value: int, name: str): str;

// Test 3: Function with optional parameters
var formatter: fn(text: str, indent?: int): str;

// Test 4: Function returning function
var factory: fn(multiplier: int): fn(int): int;

// Test 5: Higher-order function type
var mapper: fn(items: [int], transform: fn(int): str): [str];

// Test 6: Lambda with explicit type
var lambda: fn(int): int = fn(x: int): int {
    return x * 2;
};

// Test 7: Function declaration with new syntax
fn createAdder(base: int): fn(int): int {
    return fn(x: int): int {
        return base + x;
    };
}

print("Function type system test completed");