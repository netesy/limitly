print("Start");

var outer = 0;
while (outer < 1) {
    print("outer = {outer}");
    var inner = 0;
    while (inner < 1) {
        print("inner = {inner}");
        inner = inner + 1;
    }
    print("after inner loop");
    outer = outer + 1;
}

print("End");
