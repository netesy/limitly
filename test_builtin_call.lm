// Test calling builtin functions

print("Starting test...");

fn add(x: int, y: int) : int {
    return x + y;
}

print("Function defined");

// Test calling len function (which should work)
var strs = "hello";
var length = len(strs);
print("Length: " + length);

print("Test completed");