import std.graphics.index as graphics;
import std.scene.index as scene;
import std.ui.index as ui;

print("Testing Simplified UI/Scene/Graphics...");

var c = graphics.Color(r=1.0, g=0.0, b=0.0, a=1.0);
print("Color R: ");
print(c.r);

var s = scene.Scene();
print("Scene camera initialized.");

var app = ui.Application(n="TestApp");
print("Application Name: ");
print(app.name);

print("Test Passed!");
