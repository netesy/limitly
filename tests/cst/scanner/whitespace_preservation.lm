// Test whitespace preservation in CST scanner
// This file tests that the enhanced scanner preserves all whitespace tokens

// Simple expressions with various whitespace patterns
var x    =    42;     // Multiple spaces
var y	=	24;       // Tabs

assert(x == 42, "Variable x should be 42");
assert(y == 24, "Variable y should be 24");

// Test newlines and indentation
print("Testing whitespace preservation");

// Test trailing whitespace and empty lines


var z = x + y;   

assert(z == 66, "x + y should be 66 (42 + 24)");
print("Result: " + str(z));

// Test comment preservation
/* Block comment with
   multiple lines */

// Line comment at end