// Test invalid syntax recovery in CST parser
// This file contains syntax errors to test error recovery and partial node creation

// Valid code first
var x = 42;

// Missing semicolon - should recover
var y = 24
var z = x + y;  // Should parse correctly after recovery

// Missing closing brace - should create error/missing node
if (x > 0) {
    print("positive");
    // Missing closing brace

// Should continue parsing after synchronization
print("After error");

// Should still parse valid code after errors
print("Final statement");