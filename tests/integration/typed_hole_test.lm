// Test Typed Holes End-to-End Parsing and Query
fn add(a: int, b: int): int {
    return a + b;
}

fn test_holes() {
    var x: int = 10;
    var y: int = ?; // Typed hole parsed from source ? token
    assert(x == 10, "x is 10");
}

fn main() {
    test_holes();
    print("Typed holes integration test PASSED");
}

main();
