// VM Error Handling Opcodes Test
// This test focuses on the basic VM error handling operations

print("=== VM Error Handling Opcodes Test ===")

// Test basic error and success value creation
// These would normally be generated by the compiler for err() and ok() calls

// For now, var's test with simple operations that might generate errors
print("Testing basic operations...")

// Test division (which should work normally)
var a = 10;
var b = 2;
var result = a / b;
print("10 / 2 = " + result);

// Test with zero (this should work for now, but will generate error when built-in errors are implemented)
var c = 10;  
var d = 0;
var result2 = (c / d) ?else{ 0 };
print("10 / 0 = " + result2);

print("Basic VM error opcode infrastructure is ready")
print("Full error handling will be tested when compiler integration is compvare")

print("=== VM Error Opcodes Test Compvare ===")