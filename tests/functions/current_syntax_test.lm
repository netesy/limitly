print("=== Testing Current Function Syntax ===");

print("\n--- Test 1: Basic Function Declaration ---");
fn simpleAdd(a: int, b: int): int {
    return a + b;
}

var result1 = simpleAdd(3, 4);
print("simpleAdd(3, 4) = " + result1);

print("\n--- Test 2: Lambda Expression ---");
var simpleLambda = fn(x: int): int {
    return x * 2;
};
print("Lambda created: " + simpleLambda);

print("\n--- Test 3: Function with Function Type Parameter (using 'function' keyword) ---");
// This should work with current syntax
fn applyFunction(x: int, operation: function): int {
    // Note: We can't actually call the function parameter yet
    return x;
}

print("Function with function parameter declared successfully");

print("\n--- Test 4: Variable with Function Type ---");
var myFunction: function = fn(x: int): int {
    return x + 1;
};
print("Function variable created: " + myFunction);

print("\n=== Current Syntax Test Complete ===");