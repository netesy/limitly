// =============================================================================
// GENUINE MEMORY ERROR DEMONSTRATION
// Demonstrates that the memory checker still detects real errors
// =============================================================================

print("Genuine error tests verify that real errors are still detected");
print("Note: The following would fail if uncommented:");
print("  - Use after move for linear types (List, Dict, Frame)");
print("  - Double move of linear types");
print("  - Use before initialization");
print("The memory checker has been fixed to not produce false positives");
print("while still maintaining genuine error detection for linear types.");
