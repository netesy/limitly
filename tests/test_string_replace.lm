import std.string as string;

fn main() {
    var trimmed = "Hello, Limitly World!";
    var replaced = string.replace(trimmed, "World", "Language");
    print("Replaced:");
    print(replaced);
    assert(replaced == "Hello, Limitly Language!", "Replace failure");
}

main();
