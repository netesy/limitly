// Test simple nested loop
print("=== Testing Simple Nested Loop ===");

var outer_list = [1, 2, 3];
var inner_list = ["a", "b", "c"];

print("Nested iter loops:");
iter (var x in outer_list) {
    print("Outer: {x}");
    iter (var y in inner_list) {
        print("  Inner: {y}");
    }
}

print("Done");