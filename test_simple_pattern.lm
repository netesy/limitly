// Simple pattern matching test

fn testMatch(value: int): str {
    match (value) {
        5 => "Five",
        10 => "Ten",
        _ => "Other"
    }
}

print(testMatch(5));
print(testMatch(10));
print(testMatch(7));