// Test list append operations
print("=== List Append Test ===");

// Create an empty list
var myList = [];
print("Empty list: " + myList);

// Note: Direct list.append() method calls are not implemented yet
// But we can test list creation with elements
var numbers = [1, 2, 3];
print("Numbers list: " + numbers);

var strings = ["hello", "world"];
print("Strings list: " + strings);

var mixed = [1, "hello", true];
print("Mixed list: " + mixed);