// Test: Simple counter module frame
frame Counter {
    pub var count: int;

    pub init() {
        self.count = 0;
    }

    pub fn increment() {
        self.count = self.count + 1;
    }

    pub fn get_count(): int {
        return self.count;
    }
}
