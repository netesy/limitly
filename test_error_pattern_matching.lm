// Test error pattern matching with builtin function errors

print("Testing error pattern matching with builtin functions...");

// Test error pattern matching
var result = len(nil);
print("len(nil) result: " + result);

// Test with match expression (if supported)
match(result) {
    val(length) => {
        print("Success: length is " + length);
    },
    err(error) => {
        print("Error occurred: " + error);
    }
}

print("Error pattern matching test completed!");