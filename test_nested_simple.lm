print("=== Simple Nested Function Test ===");

fn outerFunction(): function {
    return fn(x: int): int {
        return x + 1;
    };
}

print("Calling outerFunction...");
var innerFunc = outerFunction();
print("Got inner function: " + innerFunc);

print("\n=== Test Complete ===");