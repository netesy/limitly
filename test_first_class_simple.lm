// Simple first-class function test
fn add(a: int, b: int): int {
    return a + b;
}

print("Function declared");

// Try to assign function to variable
var addFunc = add;
print("Function assigned to variable");

// Try to call the function through the variable
var result = addFunc(3, 4);
print("Result: " + result);