import std.ui.widget as widget;

frame Padding {
    pub var child: widget.Widget;
    pub var value: float;

    pub init(ch: widget.Widget = widget.Widget(), v: float = 0.0) {
        self.child = ch;
        self.value = v;
    }

    pub fn build(): widget.Widget {
        return self;
    }
}
