import std.ui.widget as widget;

frame GridView {
    pub var children: [widget.Widget];

    pub init(ch: [widget.Widget] = [widget.Widget()]) {
        self.children = ch;
    }

    pub fn build(): widget.Widget {
        return widget.Widget();
    }
}
