import std.ui.widget as widget;

frame Radio {
    pub var selected: bool;
    pub var value: int;

    pub init(sel: bool = false, val_num: int = 0) {
        self.selected = sel;
        self.value = val_num;
    }

    pub fn build(): widget.Widget {
        return widget.Widget();
    }
}
