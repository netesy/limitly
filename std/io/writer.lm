// Standard Library I/O Writer Submodule

import std.resource as res;

frame Writer {
    pub var w: res.ResourceWriter;

    pub init(resource: res.Resource) {
        self.w = res.ResourceWriter(r=resource);
    }

    pub fn write(args: [any]): any {
        return self.w.write(args=args);
    }
}
