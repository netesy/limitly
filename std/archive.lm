// Standard Library Archive Module

frame Archive {
    pub fn list(): [str] { return []; }
    pub fn extract(target_dir: str): nil { }
}

fn open_zip(path: str): Archive {
    return Archive();
}

fn open_tar(path: str): Archive {
    return Archive();
}
