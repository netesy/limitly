// Standard Library Unicode Rune Module

fn char_code(ch: str): int {
    var alphabet = " abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
    var i = 0;
    var l = len(alphabet);
    while (i < l) {
        if (_builtin_substring(alphabet, i, i + 1) == ch) {
            return i;
        }
        i = i + 1;
    }
    return 999;
}

frame Rune {
    pub var value: str;

    pub init(value: str) {
        if (len(value) > 0) {
            self.value = _builtin_substring(value, 0, 1);
        } else {
            self.value = "";
        }
    }

    pub fn to_string(): str {
        return self.value;
    }

    pub fn compare(other: Rune): int {
        if (self.value == other.value) return 0;
        var code_a = char_code(self.value);
        var code_b = char_code(other.value);
        if (code_a < code_b) return -1;
        if (code_a > code_b) return 1;
        return 0;
    }
}
