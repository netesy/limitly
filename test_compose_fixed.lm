// Test compose function with proper variable names

print("Starting compose test...");

fn add(x: int, y: int) : int {
    return x + y;
}

fn double(x: int) : int {
    return x * 2;
}

print("Functions defined");

// Test compose function
print("Calling compose...");
var composed = compose(double, add);
print("Compose result: " + composed);
print("Compose result type: " + typeOf(composed));

print("Test completed");